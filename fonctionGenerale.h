#ifndef fonctionM
    #define fonctionM
    #include fonctionMenu.h //contient les bibliothèque
#endif

void title();
int charToInt(char c);
char intToChar(int i);
void clear();
unsigned long getTime();